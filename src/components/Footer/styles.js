import colors from '../../colors'

const breakpoint = '400px'

export default {
    container: {
        backgroundColor: 'white',
        color: colors.darkGrey,
        padding: 20,
        textAlign: 'right',
        fontSize: 14,
    }
}
